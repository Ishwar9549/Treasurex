# Server configuration
server:
  port: 9090  # Port on which the service runs

spring:
  application:
    name: user-service-2.0.11  # Application name

  # Datasource configuration for PostgreSQL
  datasource:
    url: jdbc:postgresql://localhost:5432/treasurex_user_service_2  # DB URL
    driver-class-name: org.postgresql.Driver
    username: postgres  # DB username
    password: root      # DB password

  # Email (SMTP) configuration for sending OTPs / notifications
  mail:
    host: smtp.gmail.com
    port: 587
    username: imnavi9999@gmail.com
    password: axlo gwlk pxjx rjmt
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Custom JWT configuration
treasurex:
  jwt:
    secret-key: "uN7t8k2pBqQ9yD4xLrC6vE0jZ5sF3wX1mT8oR2lV7nP4cA9hJ3yG6bM5dQ0zK8sW"
    registration-expiry-minutes: 15  # OTP / registration token expiry
    access-expiry-minutes: 60       # Access token expiry

# MyBatis configuration
mybatis:
  mapper-locations: classpath:mappers/*.xml  # XML mapper files
  type-aliases-package: com.treasurex.userservice.model  # Model package
