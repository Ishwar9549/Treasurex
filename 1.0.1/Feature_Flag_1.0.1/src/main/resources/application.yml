server:
  port: 9091

spring:
  application:
    name: Treasurex Feature_Flag Service 

  datasource:
    url: jdbc:mysql://localhost:3306/Treasurex_2?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

