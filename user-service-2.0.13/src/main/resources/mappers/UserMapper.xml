<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.treasurex.userservice.mapper.UserMapper">

	<!-- ===================== RESULT MAP ===================== -->
	<resultMap id="UserResultMap"
		type="com.treasurex.userservice.model.User">

		<id column="id" property="id" />
		<result column="type_of_user" property="userType" />
		<result column="phone_number" property="phoneNumber" />
		<result column="phone_verified" property="isPhoneVerified" />
		<result column="password" property="passwordHash" />
		<result column="email" property="email" />
		<result column="email_verified" property="isEmailVerified" />

		<result column="otp" property="otp" />
		<result column="otp_purpose" property="otpPurpose"
			javaType="com.treasurex.userservice.enums.OtpPurpose"
			typeHandler="org.apache.ibatis.type.EnumTypeHandler" />
		<result column="otp_channel" property="otpChannel"
			javaType="com.treasurex.userservice.enums.OtpChannel"
			typeHandler="org.apache.ibatis.type.EnumTypeHandler" />
		<result column="otp_attempts" property="otpAttemptCount" />
		<result column="otp_expiry" property="otpExpiryTime" />

		<result column="user_name" property="username" />
		<result column="mpin_hash" property="mpinHash" />

	</resultMap>

	<!-- ===================== SELECT QUERIES ===================== -->
	<select id="findByPhoneNumber" resultMap="UserResultMap">
		SELECT * FROM users WHERE phone_number = #{phoneNumber}
	</select>

	<select id="findByEmail" resultMap="UserResultMap">
		SELECT * FROM users WHERE email = #{email}
	</select>

	<select id="findByUserName" resultMap="UserResultMap">
		SELECT * FROM users WHERE user_name = #{username}
	</select>

	<select id="existsByUserName" resultType="boolean">
		SELECT CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END
		FROM users
		WHERE user_name = #{username}
	</select>

	<!-- ===================== INSERT ===================== -->
	<insert id="save"
		parameterType="com.treasurex.userservice.model.User"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO users (
		type_of_user, phone_number, phone_verified, password, email, email_verified,
		otp, otp_purpose, otp_channel, otp_attempts, otp_expiry, user_name,
		mpin_hash
		)
		VALUES (
		#{userType}, #{phoneNumber}, #{isPhoneVerified}, #{passwordHash}, #{email},
		#{isEmailVerified},
		#{otp}, #{otpPurpose, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
		#{otpChannel, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
		#{otpAttemptCount}, #{otpExpiryTime}, #{username}, #{mpinHash}
		)
	</insert>

	<!-- ===================== UPDATE ===================== -->
	<update id="update"
		parameterType="com.treasurex.userservice.model.User">
		UPDATE users SET
		type_of_user = #{userType},
		phone_number = #{phoneNumber},
		phone_verified = #{isPhoneVerified},
		password = #{passwordHash},
		email = #{email},
		email_verified = #{isEmailVerified},
		otp = #{otp},
		otp_purpose = #{otpPurpose, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
		otp_channel = #{otpChannel,
		typeHandler=org.apache.ibatis.type.EnumTypeHandler},
		otp_attempts = #{otpAttemptCount},
		otp_expiry = #{otpExpiryTime},
		user_name = #{username},
		mpin_hash = #{mpinHash}
		WHERE id = #{id}
	</update>

</mapper>
