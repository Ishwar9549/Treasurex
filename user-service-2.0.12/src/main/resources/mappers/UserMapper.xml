<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.treasurex.userservice.mapper.UserMapper">

	<!-- ===================== RESULT MAP ===================== -->

	<resultMap id="UserResultMap"
		type="com.treasurex.userservice.model.User">

		<id column="id" property="id" />

		<result column="type_of_user" property="typeOfUser" />
		<result column="phone_number" property="phoneNumber" />
		<result column="phone_verified" property="phoneVerified" />
		<result column="password" property="password" />
		<result column="email" property="email" />
		<result column="email_verified" property="emailVerified" />

		<result column="otp" property="otp" />

		<!-- Enum: OtpPurpose -->
		<result column="otp_purpose" property="otpPurpose"
			javaType="com.treasurex.userservice.enums.OtpPurpose"
			typeHandler="org.apache.ibatis.type.EnumTypeHandler" />

		<!-- Enum: OtpChannel -->
		<result column="otp_channel" property="otpChannel"
			javaType="com.treasurex.userservice.enums.OtpChannel"
			typeHandler="org.apache.ibatis.type.EnumTypeHandler" />

		<result column="otp_attempts" property="otpAttempts" />
		<result column="otp_expiry" property="otpExpiry" />

		<result column="user_name" property="userName" />
		<result column="mpin_hash" property="mpinHash" />

	</resultMap>


	<!-- ===================== SELECT QUERIES ===================== -->

	<select id="findByPhoneNumber" resultMap="UserResultMap">
		SELECT
		id,
		type_of_user,
		phone_number,
		phone_verified,
		password,
		email,
		email_verified,
		otp,
		otp_purpose,
		otp_channel,
		otp_attempts,
		otp_expiry,
		user_name,
		mpin_hash
		FROM users
		WHERE phone_number = #{phoneNumber}
	</select>


	<select id="findByEmail" resultMap="UserResultMap">
		SELECT
		id,
		type_of_user,
		phone_number,
		phone_verified,
		password,
		email,
		email_verified,
		otp,
		otp_purpose,
		otp_channel,
		otp_attempts,
		otp_expiry,
		user_name,
		mpin_hash
		FROM users
		WHERE email = #{email}
	</select>


	<select id="findByUserName" resultMap="UserResultMap">
		SELECT
		id,
		type_of_user,
		phone_number,
		phone_verified,
		password,
		email,
		email_verified,
		otp,
		otp_purpose,
		otp_channel,
		otp_attempts,
		otp_expiry,
		user_name,
		mpin_hash
		FROM users
		WHERE user_name = #{userName}
	</select>


	<select id="existsByUserName" resultType="boolean">
		SELECT CASE
		WHEN COUNT(*) > 0 THEN TRUE
		ELSE FALSE
		END
		FROM users
		WHERE user_name = #{userName}
	</select>


	<!-- ===================== INSERT ===================== -->

	<insert id="save"
		parameterType="com.treasurex.userservice.model.User"
		useGeneratedKeys="true" keyProperty="id">

		INSERT INTO users (
		type_of_user,
		phone_number,
		phone_verified,
		password,
		email,
		email_verified,
		otp,
		otp_purpose,
		otp_channel,
		otp_attempts,
		otp_expiry,
		user_name,
		mpin_hash
		)
		VALUES (
		#{typeOfUser},
		#{phoneNumber},
		#{phoneVerified},
		#{password},
		#{email},
		#{emailVerified},
		#{otp},
		#{otpPurpose, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
		#{otpChannel, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
		#{otpAttempts},
		#{otpExpiry},
		#{userName},
		#{mpinHash}
		)

	</insert>


	<!-- ===================== UPDATE ===================== -->

	<update id="update"
		parameterType="com.treasurex.userservice.model.User">

		UPDATE users
		SET
		type_of_user = #{typeOfUser},
		phone_number = #{phoneNumber},
		phone_verified = #{phoneVerified},
		password = #{password},
		email = #{email},
		email_verified = #{emailVerified},
		otp = #{otp},
		otp_purpose = #{otpPurpose, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
		otp_channel = #{otpChannel,
		typeHandler=org.apache.ibatis.type.EnumTypeHandler},
		otp_attempts = #{otpAttempts},
		otp_expiry = #{otpExpiry},
		user_name = #{userName},
		mpin_hash = #{mpinHash}
		WHERE id = #{id}

	</update>

</mapper>
